{
    "configurations": [
        {
            "name": "Launch Edge",
            "request": "launch",
            "type": "msedge",
            "url": "https://localhost:5173",
            "webRoot": "${workspaceFolder}/ardonite.client"
        }
    ]
}